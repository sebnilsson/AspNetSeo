namespace AspNetSeo;

/// <summary>
/// Specifies which attribute a custom meta tag key should use when rendered.
/// </summary>
public enum CustomMetaAttributeKey
{
    /// <summary>Render the key using the name attribute.</summary>
    Name,

    /// <summary>Render the key using the property attribute.</summary>
    Property
}
